# Email Configuration
# Copy this file to .env and replace with your actual email service credentials

# For Zoho Mail (Recommended Configuration):
SMTP_HOST=smtp.zoho.com
SMTP_PORT=587
EMAIL_USER=your-email@yourdomain.com
EMAIL_PASS=your-secure-password

# Alternative Zoho settings:
# SMTP_PORT=465 (for SSL - may require additional config)

# For Gmail:
# 1. Enable 2-factor authentication on your Gmail account
# 2. Generate an "App Password" (not your regular password)
# 3. Use that app password in EMAIL_PASS below
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# EMAIL_USER=your-gmail@gmail.com
# EMAIL_PASS=your-app-password

# Company email where contact form submissions will be sent
COMPANY_EMAIL=your-company@yourdomain.com

# Website URL for email links (production)
WEBSITE_URL=https://yourdomain.com

# Development Settings
NODE_ENV=development
PORT=3000

# Production Settings (for deployment)
# NODE_ENV=production
# PORT=80
# WEBSITE_URL=https://ivanaresources.co.zw

# Security Notes:
# - Never commit .env files with real credentials to version control
# - Use strong, unique passwords for email accounts
# - Consider using app-specific passwords for enhanced security
# - For production, use environment variables or secure secret management
